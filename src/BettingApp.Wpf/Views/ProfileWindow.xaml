<Window x:Class="BettingApp.Wpf.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Můj profil"
        Width="620"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}"
        FontFamily="{StaticResource AppFontFamily}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="24">
        <Border Padding="22"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock FontSize="{StaticResource FontSizePanelTitle}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" Text="Profil účtu" />
                    <TextBlock Margin="0,8,0,0" Foreground="{StaticResource SecondaryTextBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,22,0,0">
                    <TextBlock Foreground="{StaticResource SecondaryTextBrush}" Text="Role" />
                    <TextBlock Margin="0,6,0,12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" Text="{Binding RolesDisplay}" />
                    <TextBlock Foreground="{StaticResource SecondaryTextBrush}" Text="Uživatelské jméno" />
                    <TextBox Margin="0,6,0,12" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Foreground="{StaticResource SecondaryTextBrush}" Text="E-mail" />
                    <TextBox Margin="0,6,0,8" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Foreground="{StaticResource SecondaryTextBrush}" Text="{Binding EmailConfirmed, StringFormat=Aktivovaný e-mail: {0}}" />

                    <Border Margin="0,18,0,0"
                            Padding="12"
                            Background="#A30F172A"
                            BorderBrush="#22F8FAFC"
                            BorderThickness="1"
                            CornerRadius="18"
                            Visibility="{Binding HasPreview, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock Foreground="{StaticResource PrimaryTextBrush}" FontFamily="Consolas" Text="{Binding PreviewText}" TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,18,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Width="120" Height="40" Margin="0,0,12,0" Click="CancelButton_Click" Content="Zavřít" />
                    <Button Width="140" Height="40" Click="SaveButton_Click" Content="Uložit profil" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
